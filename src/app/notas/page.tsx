import { NotesView } from "@/components/notes/NotesView";

export const metadata = {
  title: "Caderno de Notas — Entrelinhas",
  description: "Ensaios, sínteses e anotações aprofundadas dos seus estudos.",
};

export default function NotasPage() {
  return <NotesView />;
}
