import { JournalView } from "@/components/notes/JournalView";

export const metadata = {
  title: "Diário Pessoal — Entrelinhas",
  description: "Registro de pensamentos diários, sentimentos, conflitos e lampejos de ideias.",
};

export default function DiarioPage() {
  return <JournalView />;
}
